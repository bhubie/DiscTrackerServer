{
  "name": "disc-track-server",
  "version": "1.1.0",
  "description": "Server app for Disck Tracker",
  "license": "MIT",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongoose": "^5.3.15"
  }
}
